# Especulai — Variáveis de Ambiente
# Copie para .env na raiz do projeto e preencha os valores

# API
API_PORT=8000
ENVIRONMENT=development

# CORS (separar por vírgula)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Artefatos ML (relativos à raiz do projeto)
MODEL_PATH=ml/artifacts/modelo_definitivo.joblib
PREPROCESSOR_PATH=ml/artifacts/preprocessador.joblib

# Dados (relativos à raiz do projeto)
DATA_DIR=data

# Frontend (Vite)
VITE_API_URL=http://localhost:8000
